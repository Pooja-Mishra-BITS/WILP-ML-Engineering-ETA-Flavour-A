# Model comparison evidence

Capture reports/model_comparison.md and the actual output from:

~~~text
python -m pipeline.train
~~~

The evidence must show both models, feature representation, hyperparameters, MAE, RMSE, R², and selection.
